{
  "name": "secret-calculator-chat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:db": "docker compose up -d",
    "dev:local": "concurrently \"npm run dev:memory --prefix server\" \"npm run dev --prefix client\"",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "dependencies": {
    "cors": "^2.8.6"
  }
}
